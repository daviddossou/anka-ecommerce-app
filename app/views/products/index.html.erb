<h1>Products</h1>
<ul>
  <% @products.each do |product| %>
    <li>
      <%= link_to product.name, product_path(product) %>
      <p><%= product.price %></p>
      <p><%= product.description %></p>
    </li>
  <% end %>
</ul>
